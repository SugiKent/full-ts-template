# Tasks: enable-item-detail-sheet

## フェーズ1: 基盤コンポーネントの作成

- [x] **1.1 トースト通知コンポーネント作成**
  - 場所: `apps/mobile/src/utils/toast.ts`
  - ライブラリ: `react-native-toast-message` を使用
  - 成功/エラー/情報の3種類をサポート
  - 検証: コンポーネントが正しくレンダリングされる

- [x] **1.2 useItemMutation フック作成**
  - 場所: `apps/mobile/src/hooks/useItemMutation.ts`
  - アイテム更新・削除のAPI呼び出しをラップ
  - ローディング状態とエラー処理を含む
  - 検証: TypeScript型チェック通過

- [x] **1.3 useStepMutation フック作成**
  - 場所: `apps/mobile/src/hooks/useStepMutation.ts`
  - ステップ作成・完了トグル・削除のAPI呼び出しをラップ
  - 楽観的更新ロジックを含む
  - 検証: TypeScript型チェック通過

- [x] **1.4 useAiSuggestion フック作成**
  - 場所: `apps/mobile/src/hooks/useAiSuggestion.ts`
  - AIステップ提案のAPI呼び出しをラップ
  - ローディング状態とフォールバック処理
  - 検証: TypeScript型チェック通過

## フェーズ2: ステップ機能のAPI連携

- [x] **2.1 ステップ完了トグルのAPI連携**
  - ItemDetailSheet で useStepMutation を使用
  - 楽観的更新: UI即時反映 → API呼び出し → エラー時ロールバック
  - 検証: ステップタップで完了状態がサーバーに反映される

- [x] **2.2 ステップ追加のAPI連携**
  - 新規ステップ作成時にAPI呼び出し
  - 追加後、リストに新しいステップを反映
  - 検証: ステップ追加がサーバーに保存される

- [x] **2.3 ステップ削除のAPI連携**
  - スワイプ削除時にAPI呼び出し
  - 楽観的更新でUIを即時反映
  - 検証: ステップ削除がサーバーに反映される

## フェーズ3: AIステップ提案の実装

- [x] **3.1 AiSuggestionSheet コンポーネント作成**
  - ItemDetailSheet 内にインライン実装
  - BottomSheetModal 内のスクロールビューで表示
  - スケルトンローディング状態を含む
  - 検証: コンポーネントが正しくレンダリングされる

- [x] **3.2 AI提案取得機能の実装**
  - 「もっとステップを提案」ボタンから useAiSuggestion を呼び出し
  - 既存ステップと完了済みステップを提案APIに送信
  - ローディング中はスケルトンUIを表示
  - 検証: AI APIが呼び出され、提案が表示される

- [x] **3.3 提案ステップの選択・追加機能**
  - チェックボックスで複数選択可能
  - 「追加」ボタンで選択したステップをサーバーに保存
  - 追加後は提案シートを閉じる
  - 検証: 選択したステップがリストに追加される

## フェーズ4: アイテム編集・削除の実装

- [x] **4.1 アイテム編集機能**
  - シートヘッダーのインライン編集
  - 編集モード切替とキャンセル機能
  - 保存時にAPI呼び出し
  - 検証: タイトル変更がサーバーに反映される

- [x] **4.2 アイテム削除機能**
  - 削除ボタン押下で確認ダイアログ表示
  - 確認後にAPI呼び出しとシート閉じる
  - 親コンポーネントへのコールバック
  - 検証: アイテム削除がサーバーに反映され、リストから消える

## フェーズ5: 状態同期と親コンポーネント更新

- [x] **5.1 HomePageContent のコールバック更新**
  - `onItemUpdate`, `onItemDelete`, `onStepsChange`, `onSuggestionsChange` props を ItemDetailSheet に追加
  - 親の状態を更新するコールバック実装
  - 検証: シート内の操作が親のリストに反映される

- [x] **5.2 エラーハンドリングの統合**
  - 各API呼び出しでトースト通知を表示
  - 楽観的更新のロールバック処理
  - 検証: エラー発生時にトーストが表示される

## フェーズ6: 翻訳キーの追加

- [x] **6.1 i18n 翻訳キー追加**
  - モバイルアプリはi18nシステムを使用していないためスキップ
  - UIテキストは日本語でハードコード済み

## フェーズ7: 検証

- [x] **7.1 ビルド確認**
  - `pnpm run typecheck` ✓
  - `pnpm run lint` ✓
  - `pnpm run build` ✓

- [ ] **7.2 動作確認**
  - iOS Simulator での手動テスト
  - 各操作がサーバーに反映されることを確認
